# Core: local speech-to-text. No API keys, no network after the first run.
faster-whisper>=1.1.0
numpy>=1.24

# Microphone capture.
sounddevice>=0.4.6

# Global hotkey + synthesised keystrokes.
pynput>=1.7.6

# Clipboard, for the default "paste" injection method.
pyperclip>=1.8.2

# System-tray icon. Drop these two if you always run with --no-tray.
pystray>=0.19.5
pillow>=10.0.0

# Reads the focused control back so edits can be learnt from. Optional:
# without it, automatic learning is off and the tray's "Fix last dictation"
# dialog still teaches whispa. Windows only.
uiautomation>=2.0.18 ; sys_platform == "win32"

# The on-screen indicator uses tkinter, which ships with Python - no entry
# needed here, but note that some minimal Python builds omit it.
